draw_text(RES_W / 2, 20, "Etapa: "+string(etapa + 1));

if(room == rm_SeqTutorial)
{
	//draw_text(RES_W / 2, 60, timer);
}

if (etapa >= 17)
{
	//draw_text(RES_W / 2, 40, "msg bigmart: "+string(msg_bigmart));
}